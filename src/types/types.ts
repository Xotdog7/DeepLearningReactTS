export interface MessageUser {
  id: number;
  name: string;
}
export interface MessageText {
  id: number;
  message: string;

}

export interface IPosts {
  id: number;
  message: string;
  likeCount: number;
}
